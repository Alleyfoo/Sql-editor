# Handoff - 2026-07-06

## What was completed

UX smoke test of the running Streamlit app, done via Playwright DOM checks
(verified text/DOM, never screenshots — image reads are unsupported on this
model and crash the session). Previous attempt at this same smoke test crashed
the agent because it tried to `Read` a screenshot PNG. This run avoided all
image reads.

Full suite already green before this session:

```bash
.venv\Scripts\python.exe -m pytest tests/ -q
# 280 passed
```

### Smoke-test results (21/21 + 1 live LLM check, all green)

Launched `streamlit run streamlit_app.py` on port 8639 (headless) and drove it
with Playwright (`C:\Users\pertt\AppData\Local\Temp\pw-smoke.js`).

Verified, by DOM text only:

- **Main tabs**: 3 present in order `Workflow`, `Studio`, `LLM SQL Assistant`
  (plus Studio's inner `Schema`/`Compose`/`History` tabs).
- **Default landing**: `Workflow` tab is `aria-selected=true` on first load
  (confirms `fad39ce` / `d2e996f` tab work).
- **Workflow tab**: "Guided tour" heading + step cards render; "Load demo
  dataset" primary CTA works (button → "✓ Demo dataset loaded"); step-2
  "Open Studio" button visible.
- **Studio tab**: activates on click; content renders; inner
  Schema/Compose/History tabs present.
- **LLM SQL Assistant tab**: activates on click; safety banner, Heuristic
  column header, Run comparison button, schema-aware example chips all render.
- **Offline heuristic path** (no model needed): clicking the "count rows by
  customer_id" chip prefills the ask input; the heuristic produces a SELECT
  code block with no network call. Run comparison becomes enabled.
- **Round-trip**: Workflow → Studio → LLM → Workflow tab switching works
  (the `key="main_tabs"` fix from `d2e996f`).
- **Error sweep**: no `stException` blocks; no browser console errors; no
  uncaught page errors.

### Live LLM path (bonus — a model was actually connected)

The app's status pill reported `LLM: connected · qwen3.5:9b` (local Ollama on
default host). Pressed **Run comparison** for real → model accepted in ~7.5 s
and emitted a valid plan; generated SQL:

```sql
SELECT "customer_id", COUNT(*) AS "order_count" FROM "data" GROUP BY "customer_id"
```

Plan diff (heuristic vs LLM) rendered. No exceptions. So the full heuristic-vs-LLM
comparison, the JSON-plan-not-SQL trust boundary, and the live Ollama client all
work end to end.

### Cleanup

Removed 5 stale atomic-write editor backups that had accumulated under
`src/streamlit_app/pages/` (`llm_assistant.py.tmp.5956.*`). They were leftover
from the earlier emoji-removal commit (`e52c734`) — already-committed work, so
pure junk. (Same cleanup was done once before in `3908b2c`.)

## Uncommitted changes

None after the cleanup. Only this `.handoff.md` is new.

## Known issues or failing tests

None. Test suite 280/280; smoke test 21/21 + live LLM check.

Note for future sessions: **do not `Read` image files** (PNG screenshots etc.)
on this model — it returns an API error and crashes the session. For UI
verification use Playwright DOM/text assertions, not screenshots.

## Recommended next task

The smoke test found nothing broken, so return to the product roadmap from the
prior handoff:

- Add the multi-table mode to the LLM Assistant page (raw-SQL multi-table path
  with clear safety copy). `nl_to_raw_sql` already exists in
  `src/llm/natural_language.py`; the page just needs the UI.
- Add save/load query support; the disabled Save button in the composer is still
  a placeholder.
- Add a real multi-CSV upload UI for user-provided multi-table datasets instead
  of only the bundled supply-chain demo.

If a future UX check is wanted, re-run the Playwright scripts at
`C:\Users\pertt\AppData\Local\Temp\pw-smoke.js` (and `pw-llm-run.js` for the live
model path) after starting the app on port 8639.