# Handoff — 2026-05-26 (session 6)

## What was completed this session

| Commit | What |
|--------|------|
| `fce42e0` | Feat: Streamlit Cloud support via st.secrets |
| `ef3f3e8` | Feat: Groq cloud LLM provider |
| `f9ada04` | Feat: Gemini provider + shared OpenAICompatibleClient |
| `f383650` | Fix: provider-aware probe TTL (cloud = 5 min, local = 30 s) |
| `bfe283b` | Fix: gemini-2.5-flash as default model |
| `5a5844f` | Fix: quick query buttons 4→2 columns, smaller font |
| `d49d51f` | Feat: data preview on load + clickable column chips |
| `ef81636` | Fix: data/results shown above SQL editor |
| `678df41` | Fix: multi-table preview — tab per table with column chips |

## Current state

App is fully functional on Streamlit Community Cloud using Gemini 2.5 Flash.

### What works well
- Load CSV → data table visible immediately, column chips above it
- Click column chip → ask bar prefills with type-aware question → LLM answers
- Multi-table (supply chain demo) → tab per table, same chip behaviour
- Quick queries: 2-column layout, readable labels
- Layout: data/results top, SQL editor below
- Gemini 2.5 Flash connected, 5-min probe TTL conserves free quota

### LLM providers
| Provider | Status |
|----------|--------|
| Local Ollama | ✓ works |
| Remote Ollama | ✓ works |
| Gemini (cloud) | ✓ working — use gemini-2.5-flash |
| Groq (cloud) | added but had 403 in testing |

### Streamlit Cloud setup
```toml
[llm]
provider = "gemini"
model    = "gemini-2.5-flash"
api_key  = "AIzaSy_YOUR_KEY"
```

## Uncommitted changes
- `config.yaml` — local settings only

## Known issues / nice-to-haves
1. `is_active` / `quality_pass` show `txt` badge — correct but `bool` bucket would be cleaner
2. No query save/load — sessions don't persist on Cloud
3. Multi-table composer not supported (JOIN queries come from NL Ask or quick queries only)

## Recommended next tasks
- **Save/load queries** — JSON file in project root, useful since Cloud sessions reset
- **More usability polish** — user moving to a related pandas-based project in another folder

## Running locally
```
cd C:\Users\pertt\sql-editor
.venv\Scripts\activate
streamlit run streamlit_app.py
```
